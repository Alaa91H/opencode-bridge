# دورة الإصدارات والنشر لبوت OpenCode

يحتفظ المشروع بمستودع Git محلي داخل `/home/ubuntu/opencode-bridge`. فرع **`main`** هو المصدر المرجعي للإصدارات القابلة للنشر، بينما تُنفّذ التغييرات اللاحقة على فروع قصيرة العمر ثم تُراجع قبل دمجها في `main`. لا تتضمن الشجرة أي أسرار أو جلسات أو مرفقات تشغيلية؛ تبقى هذه البيانات محلية ضمن `.env` و`runtime/` وقاعدة `sessions.db`.

| المرحلة | الأمر أو الإجراء | الضابط الأساسي |
|---|---|---|
| بدء تعديل | `git checkout main && git pull --ff-only` عند ربط مستودع بعيد مستقبلًا، ثم `git checkout -b feature/اسم-التغيير` | لا تعمل على `main` مباشرة. |
| تحقق قبل الدمج | `scripts/verify.sh` | لا بناء ولا تثبيت حزم ولا تغيير لبيانات التشغيل. |
| مراجعة التغيير | `git diff` ثم `git status` | لا تضف `.env` أو `runtime/` أو `sessions.db`. |
| إصدار | من `main`: `scripts/release.sh 1.0.1 "ملخص واضح"` | ينشئ التزامًا ووسمًا دلاليًا ثم ينشر الوسم غير القابل للتبديل. |
| فحص ما بعد النشر | `systemctl --user status opencode-bridge-telegram.service --no-pager` | يتحقق سكربت النشر أيضًا من صحة خدمة OpenCode المحلية. |
| تراجع | `scripts/rollback.sh` أو `scripts/rollback.sh v1.0.0` | يمنع التراجع عند وجود مهمة طابور جارية. |

> لا يشغّل سير العمل أي أوامر بناء أو تجميع أو تثبيت اعتماديات على الخادم. تقتصر عملية التحقق على فحص صياغة السكربتات، ترجمة Python، سلامة الاعتماديات المثبتة، واختبارات الوحدة.

## النسخ الدلالي

يستخدم المشروع الصيغة `MAJOR.MINOR.PATCH`. تُرفع **PATCH** للإصلاحات المتوافقة، و**MINOR** للقدرات الجديدة المتوافقة، و**MAJOR** للتغييرات غير المتوافقة. يُحدَّث ملف `VERSION` وسجل `CHANGELOG.md` عند إنشاء كل إصدار.

## آلية النشر

يأخذ `scripts/deploy.sh` نسخة أرشيفية من التزام الإصدار السابق في `/home/ubuntu/opencode-backups/releases` قبل الانتقال إلى المرجع المطلوب. بعد التحقق يعيد تحميل وحدات systemd عند الحاجة، ويعيد تشغيل خدمة تيليغرام فقط بعد التأكد من عدم وجود مهمة `running` في الطابور. عند فشل التحقق أو فشل عودة الخدمة للحالة النشطة، يعيد السكربت تلقائيًا آخر التزام كان يعمل ويعيد تشغيل الخدمة.

يسجل النشر الناجح المرجع الحالي والسابق في `runtime/`، وهو مسار مستبعد من Git. لذلك يعتمد التراجع الافتراضي على المرجع المنشور السابق، بينما يسمح تمرير وسم أو التزام محدد عند الحاجة.

## مستودع بعيد اختياري

المستودع الحالي **محلي** ولا يرفع أي كود تلقائيًا إلى خدمة خارجية. عند الرغبة في النسخ الاحتياطي أو التعاون، يمكن ربط مستودع بعيد خاص لاحقًا بعد مراجعته، ثم دفع فرع `main` والوسوم صراحة. يجب عدم استخدام مستودع عام لأن ملف `.env` محلي، ولأن الكود يمثل جزءًا تشغيليًا من الخادم.
